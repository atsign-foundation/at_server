name: at_activate
description: Tool for Atsign onboarding/authentication

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  at_onboarding_cli: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
