name: install_PKAM_Keys
description: Tool to load PKAM keys into root server for testing @signs
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  at_demo_data:
    git:
      url: git@github.com:atsign-company/at_demos.git
      path: at_demo_data
      ref: master
  at_lookup:
    git:
      url: git@github.com:atsign-company/atsign_foundation.git
      path: at_lookup
      ref: dev_env

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
