import 'package:at_persistence_secondary_server/at_persistence_secondary_server.dart';

class Annotations {
  static const server = AtServerAnnotation();
  static const client = AtClientAnnotation();
}
